<resources>
    <!-- Dark theme -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white_universal</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/red</item>
        <item name="colorSecondaryVariant">@color/red</item>
        <item name="colorOnSecondary">@color/white_universal</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/black_night</item>
        <item name="android:colorBackground">@color/black_night</item>
        <item name="colorSurface">@color/black_night</item>
        <item name="colorOnBackground">@color/white_night</item>
        <item name="colorOnSurface">@color/white_night</item>

        <!-- Status bar -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/white_day</item>
    </style>
</resources>
